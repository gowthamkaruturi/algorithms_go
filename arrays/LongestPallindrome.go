package arrays

func longestPallindromeSubString(str string) {

	n := len(str)
	table = make([][]bool,n)

	for i := 0; i < len(str); i++ {
		for j := 1; j < len(str); j++ {
			flag := 1

			for k := 0; k < (j - i + 1); k++ {
				if 
			}

		}
	}
}
